document.addEventListener('DOMContentLoaded', () => {
    // Esperar 5 segundos antes de redirigir
    setTimeout(() => {
        window.location.href = 'game.html'; // Cambia la ruta según sea necesario
    }, 3000); // 5000 milisegundos = 5 segundos
});
